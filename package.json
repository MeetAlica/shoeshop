{
  "name": "shoeshop-fullstack",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd shoestore && npm install && npm run build && mkdir -p ../backend/dist && cp -r ./dist/* ../backend/dist/",
    "start": "cd backend && npm install && node server.js"
  }
}
